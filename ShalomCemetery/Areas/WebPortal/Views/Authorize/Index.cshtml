@{

    ViewData["Title"] = "Account Login";
    Layout = null;
}

<!doctype html>
<html lang="en">
<head>
    <!--  meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="shortcut icon" href="~/favicon.ico" type="image/x-icon" />
    <link rel="icon" href="~/favicon.ico" type="image/x-icon" />
    <title>ShalomCemetery</title>
    <!-- Third Party css -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,300;0,400;0,500;0,600;0,700;1,400&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <!-- Theme css -->
    <link href="assets/css/styles.css" rel="stylesheet">
    <style>
        .errormsg {
            font-size: 12px !important;
            color: red;
        }
    </style>
</head>
<body>
    <!-- Authentication Pages -->
    <div class="authentication-container">
        <!-- Form Section -->
       @*  <div class="auth-form">
            <div class="logo"><img src="assets/images/logo.svg" class="img-fliuid" alt="MBR" /></div>
            <h1 class="title" style=" margin-bottom: 1rem!important;">Log in</h1>
            @if (!string.IsNullOrEmpty(successMessage))
            {
                <div class="form-label-group" style="margin-bottom: 2rem;">
                    <span class="" style="color: green; font-size: 12px !important;">@successMessage</span>
                </div>
            }
            else
            {
                <div class="form-label-group" style="margin-bottom: 2rem;">
                    <span class="errormsg">@ViewBag.ErrMsg</span>
                </div>
            }
            <form enctype="multipart/form-data" method="post" asp-controller="Authorization" asp-action="AccountLogin">
                <div class="form-group">
                    <div class="field-icon">
                        <input asp-for="Password" type="password"
                               class="form-control-line"
                               placeholder="Enter pin" autocomplete="off" />
                        <span class="fas fa-eye-slash form-icon toggle-password"></span>
                        <span asp-validation-for="Password" class="errormsg"></span>
                    </div>
                </div>
                <div class="form-button">
                    <button class="btn btn-primary btn-lg w-100 mb-3">Login</button>
                    <a href="/Authorization/ForgotPin" class="font-14">Forgot pin?</a>
                </div>
            </form>
        </div> *@
        <!-- Graphic image -->
        <div class="graphic-img">
            <img src="assets/images/login_graphic.svg" alt="MBR" class="img-fluid">
        </div>
        <!-- //Graphic image -->
    </div>
    <!-- //Authentication Pages -->


</body>
</html>